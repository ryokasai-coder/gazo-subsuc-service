# DESIGN BOX 作業スナップショット & 次のアクション（2026-08-24）

対象: 画像制作サブスク「DESIGN BOX」（`gazo-subsuc-service`）
本番: https://gazo-subsuc-service.vercel.app ／ GitHub: ryokasai-coder/gazo-subsuc-service
Supabase project: `searlfbcbqvqugrtuzhp` ／ デプロイ: main へ push で Vercel 自動デプロイ

最新コミット時点: `0cd8ab1`（新規テンプレ11種追加）／全て push 済み・working tree クリーン。

---

## ✅ このセッションで完了したこと（本番反映済み）

1. **利用規約（修正版）の反映**（commit `fda9c41`）
   - `lib/legal.ts` に一元化。公開ページ `/terms` を新設、トップのフッターにリンク。申込フォームも同ソースを参照。
   - 新ルール: 初回2ヶ月無料 / 無料1ヶ月目は解約不可 / 返金不可 / 著作権は当社帰属 / 納期14営業日。

2. **解約申請機能（仕様案 全ステップ）**（commit `fda9c41`）
   - 顧客導線: `/dashboard` →「契約・お支払い」(`/dashboard/billing`) →「契約情報」(`/dashboard/contract`) →「プランについて」(`/dashboard/plan`) → 解約フロー(`/dashboard/cancel`, STEP⑤〜⑩＋理由別分岐)。
   - 解約確定API `app/api/cancel/request/route.ts`（状態=解約申請済み・利用期限=当月末・理由保存＋完了メール＋Slack通知）。
   - 算出ロジック `lib/contract.ts`（無料期間/次回更新日/利用期限/実効ステータス/依頼可否）。
   - 管理画面 `/admin/users` に契約状況（解約申請/解約済み・申請日・理由・終了日）表示。
   - 契約終了後は依頼停止。日次Cron `/api/cron/reminder` で期限超過を「解約済み」へ自動確定。
   - **DBマイグレーション適用済み**（Supabase SQL Editorで実行）: `users` に contract_start_date / cancellation_status / cancel_requested_at / cancel_reason / cancel_reason_detail / service_end_date を追加。SQL: `supabase/migrations/2026-08-19-cancellation.sql`。検証: `node scripts/apply-cancellation-migration.mjs`。
   - 方針: 解約=当月末終了・次回請求なし（仕様案どおり）＋無料期間を組み込み。

3. **新規テンプレート11種（飲食・db-food-*）追加**（commit `0cd8ab1`）
   - 出典: `Downloads/DESIGN BOX 新規テンプレート11種｜画像・JSON・Driveリンク.xlsx`。
   - `lib/design-prompts.ts`: `TEMPLATE_FIELDS` 11種＋共通ビルダー `buildFoodPrompt`＋`FOOD_TEMPLATE_SPECS`＋`COMMON_FOOD_GUARDRAILS`。
   - `components/forms/ImageRequestForm.tsx`: `TEMPLATES`＋`MATERIAL_UPLOAD` に11種登録。
   - 見本画像 `public/templates/tpl-db-food-*.jpg`（11枚）。

---

## ▶ 次に行うこと（未対応・要対応）

### A. 法務まわり（優先）
1. **プライバシーポリシーの反映（未対応）**
   - 依頼のあった `Downloads\プライバシーポリシー .pdf` が**実ファイル未発見**。→ ユーザーに再保存を依頼し、`/terms` と同じ方式で公開ページ `/privacy` を新設して反映する。
2. **利用規約の社名表記の確認（要ユーザー確認）**
   - PDF原文「株式会社Funri」を誤記と判断し「株式会社Funrix」で反映済み（`lib/legal.ts` の `COMPANY_NAME`）。正式名称（例: 株式会社ファンリックス）を確定し、必要なら1箇所修正。
3. **利用規約変更に伴う既存契約顧客への通知（未対応・社内ルール）**
   - 無料期間・解約制限など顧客に実質影響する変更を含むため、通知文面を作成し既存顧客へ通知する。

### B. 解約機能の検証
4. **解約フローの通しテスト**
   - テストユーザーで STEP⑤〜⑩ を実行 → 解約完了メール受信 → `/admin/users` のステータス（解約申請済み）→ 契約終了日到来後に「解約済み」へ自動遷移（日次Cron）を確認。
   - 無料1ヶ月目のユーザーで「解約不可」ガードが出ることも確認。

### C. 新規テンプレの検証
5. **11種の生成テスト・微調整**
   - 実際に数種を生成し、複雑テンプレ（テイクアウト/イベント等）の**行ベース入力の粒度**やプロンプト文言、`orientation`（縦長/正方形）の妥当性を確認して微調整。

### D. 以前からの継続タスク（メモリ参照）
6. 「顧客が触る操作画面（UI/UX）の作り込み」が本来のゴール。上記反映後、依頼フォーム・マイページ・トップの改善を継続。

---

## 環境・運用メモ
- デプロイ: `git push origin main` → Vercel 自動デプロイ（Vercel CLI は未導入）。
- DBマイグレーション: サービスロールではDDL不可。**Supabase SQL Editor**（project `searlfbcbqvqugrtuzhp`）で実行するのが確実。ブラウザ操作でも適用可。
- メール: Resend（`lib/resend.ts`、送信元 sr.support@funrix.co.jp）。
- バックアップ先: `G:\マイドライブ\Funrix\DESIGNBOX\`（zipスナップショット）。
